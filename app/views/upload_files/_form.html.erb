<%= form_for(@upload_file) do |f| %>
  <% if @upload_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@upload_file.errors.count, "error") %> prohibited this upload file from being saved:</h2>

      <ul>
      <% @upload_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :filename %><br>
    <%= f.file_field :file, :required => true %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
